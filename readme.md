# 404SEVERWEBPAGE

Flask + SQLAlchemy project running on SQL Server.

## Requirements
- Python 3.10+
- SQL Server
- ODBC Driver 17 for SQL Server

## Setup

1. Clone repository
2. Create virtual environment
3. Install dependencies
4. Create `.env` file based on `.env.example`
5. Run application

```bash
python app.py
